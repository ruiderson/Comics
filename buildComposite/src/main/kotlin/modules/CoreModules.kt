package modules

object CoreModules {
    private const val ROOT_PATH = ":core"

    const val Data = "$ROOT_PATH:data"
    const val DesignSystem = "$ROOT_PATH:designsystem"
    const val Domain = "$ROOT_PATH:domain"
    const val Navigation = "$ROOT_PATH:navigation"
    const val Presentation = "$ROOT_PATH:presentation"

}